{
  "name": "sns-parse-extensions-repo",
  "private": true,
  "version": "0.0.0",
  "description": "sns-parse 扩展 monorepo：@sns-parse/ext-nsfw / ext-merge / ext-translate / ext-gif",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
